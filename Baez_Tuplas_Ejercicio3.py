t = (3, 6, 9)

#t[2] = 10
# Si intentamos asignar un valor al índice 2 (por ejemplo),
# el programa nos arroja una excepción "TypeError: 'tuple' object does not support item assignment"
#mostrando que las tuplas son inmutables, es decir, que no se pueden modificar
